# SPDX-Copyright-Text: 2026 Core Devices LLC
# SPDX-License-Identifier: Apache-2.0

def configure(conf):
    pass


def build(bld):
    bld.stlib(
        use=['fw_includes'],
        source=['app_session_capabilities.c'],
        target='services_normal_comm_session',
    )

    bld.env.SERVICES.append('services_normal_comm_session')
